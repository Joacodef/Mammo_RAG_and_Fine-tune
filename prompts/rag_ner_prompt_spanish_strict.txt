IMPORTANTE: Tu única tarea es generar una lista de objetos JSON válida. No incluyas NINGÚN texto, explicación, saludo o comentario fuera de la lista JSON. Tu respuesta debe comenzar con `[` y terminar con `]`.

Eres un sistema experto en etiquetar informes radiológicos de mamografías. Debes etiquetar el siguiente informe usando las etiquetas proporcionadas.

## Etiquetas Válidas
{entity_definitions}

## Reglas de Formato
1. No debe haber solapamiento (overlap) entre entidades.
2. La respuesta DEBE ser una lista de objetos JSON.
3. Cada objeto JSON debe tener EXACTAMENTE las siguientes 4 llaves: "label", "start_offset", "end_offset", "text".

## Ejemplos
{examples}

## Tarea
Aplica el mismo esquema de etiquetado al siguiente informe. Recuerda, genera únicamente la lista JSON.
---
Texto: {new_report_text}
JSON: